# Server Configuration
NODE_ENV=dev
PORT=3000

# Database Configuration
DATABASE_URL="postgresql://booking:booking_dev_password@localhost:5432/booking_db?schema=public"
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=booking
DATABASE_PASSWORD=booking_dev_password
DATABASE_NAME=booking_db

# Booking Settings
BOOKING_EXPIRATION_MINUTES=15
MAX_SEATS_PER_TRAVEL=5

# CORS Settings (comma-separated origins)
CORS_ORIGINS=http://localhost:3001,http://localhost:3000,http://localhost:8080
